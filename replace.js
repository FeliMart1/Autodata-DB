const fs=require('fs'); let c=fs.readFileSync('frontend/scripts/generate-form-equipamiento-updated.cjs', 'utf-8'); let m=c.match(/if \(category === 'Equipamiento' && /); if(m){ let m2=c.indexOf('});', m.index); if(m2>-1) { fs.writeFileSync('frontend/scripts/generate-form-equipamiento-updated.cjs', c.substring(0, m.index) + '  reactContent += \\\            </div>\\\\n          </CardContent>\\\\n        </div>\\\\n      </Card>\\\\n\\\;\\n\\n' + c.substring(m2)); console.log('success'); } }
